# Convenience script for Nix users (or anyone else) to run with `nix develop --configure`

cd $PWD/utils
mix local.rebar --force, 
mix local.hex --force
mix deps.get
mix deps.compile
mix escript.install hex livebook
